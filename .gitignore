scratch.txt
result
result-*
/builds/
.env

# Racket compiled artifacts (regenerated on every firn-build)
**/compiled/
.firn-build/
.beagle-cache/

# sops age private keys — NEVER commit these
*.age.key
keys.txt

# Editor backup files that might contain secrets
*~
*.swp
*.swo
.#*
.direnv

# Internal agent notes / status / scratch / handoffs — NOT user-facing docs.
# Keep out of git. Put private notes under docs/private/.
docs/private/
