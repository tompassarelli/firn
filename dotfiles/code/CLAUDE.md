<!--
Moved. The ~/code routing note is now a directory context in north-data:
north-data:context-dirs/code.md, registered by north-data:context-dirs.conf and
governed by the `agents` switchboard as `dir code` (`agents path code` prints
the file to edit).

It lives there because which directories carry a context file is user data, not
machine config. This pointer stays so the old location does not read as deleted
content. Editing this file changes nothing.
-->
