interface:
  display_name: "Agent Policy Distilled"
  short_description: "Author and activate agent policy safely"
  default_prompt: "Use $agent-policy-distilled to change agent instructions, skills, hooks, modules, or North activation metadata at their exact owner source."
