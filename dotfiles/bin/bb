#!/usr/bin/env bash
set -euo pipefail

bb="${BABASHKA_BIN:-$HOME/.local/state/babashka/runtime-profile/bin/bb}"

if [[ ! -x "$bb" ]]; then
  printf 'bb: no managed Babashka runtime exists at %s\n' "$bb" >&2
  printf 'run bb-runtime-update to install it\n' >&2
  exit 127
fi

exec "$bb" "$@"
