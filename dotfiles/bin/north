#!/usr/bin/env bash
# Live dev-loop tool: never store-placed (staleness charter, law 3).
set -euo pipefail
source "$HOME/.local/state/north/beagle-store.env"
exec "$HOME/code/north/main/bin/north" "$@"
